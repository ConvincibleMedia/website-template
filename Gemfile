source 'https://rubygems.org'

# CMS
gem 'dato'
#gem 'gqli'

# SSG
gem 'jekyll'
gem 'nokogiri' # Parse HTML documents
gem 'addressable' # Parse URIs
#gem 'liquid/tag/parser'
gem 'i18n'

# Development
group :development do
	gem 'transformer', :path => '../transformer'
	gem 'key_dial', :path => '../Ruby Gems/key_dial'
	gem 'pry'
end
